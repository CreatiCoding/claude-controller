# PRD — claude-controller

> 제품 요구사항 문서. 사용자 안내는 README.md, 구현 컨텍스트는 CLAUDE.md,
> 매크로패드는 ADVANCED_MACROPAD.md를 본다. 이 문서는 "무엇을, 왜, 어디까지"를 정한다.
> 마지막 갱신: 2026-09-22 (v0.1.0 기준, 실기기 엔드투엔드 검증 완료)

## 1. 배경과 문제

Claude Code를 터미널에서 돌려 두면 허가 프롬프트(예/아니오)가 뜰 때마다 작업이 멈춘다.
다른 창에서 일하거나 자리를 잠깐 비우면 프롬프트가 떠 있는 줄 모르고 시간이 샌다.

- 터미널을 계속 쳐다보고 있어야 한다 → 멀티태스킹 불가
- 세션이 여러 개면 어느 세션이 기다리는지 한눈에 안 보인다
- 공식 원격 제어(claude-desktop-buddy 등)는 데스크톱 앱 내 세션만 지원하고, 터미널 세션은 대상이 아니다

## 2. 목표

폰을 거치대에 두고 **터미널을 안 보고도** Claude Code 세션 상태를 알고, 허가에 응답한다.

성공 기준:

| 지표 | 목표 |
| --- | --- |
| 허가 요청 → 폰 화면 표시 지연 | 1초 이내 (WebSocket 푸시) |
| 폰 터치 → Claude 진행 재개 지연 | 1초 이내 |
| 시스템 장애 시 Claude Code 영향 | 없음 (fail-open, 기본 프롬프트로 복귀) |
| 첫 설치 소요 | 5분 이내, 스크립트 1회 |
| 네트워크 의존 | Wi-Fi 없이 USB 케이블만으로 동작 |

## 3. 비목표

- 폰에서 프롬프트를 타이핑하거나 대화를 이어가는 것 (원격 채팅 클라이언트가 아니다)
- Claude Code 데스크톱 앱 세션 지원 (공식 경로가 따로 있음)
- Wi-Fi/인터넷 경유 원격 접속 (인증 없는 승인 API를 외부에 열지 않는다)
- 네이티브 폰 앱 (웹 대시보드로 충분, BLE 불필요)
- 매크로패드 없이도 완결되어야 하며, 매크로패드는 선택 부가 기능이다

## 4. 사용자와 시나리오

**사용자**: 맥북에서 Claude Code CLI를 tmux/터미널로 쓰는 개인 개발자. 폰(아이폰 또는 안드로이드)이 있고 USB 케이블로 맥에 연결할 수 있다.

**S1. 허가 응답 (핵심)**
1. 사용자가 `cl`(또는 `claude`)로 세션을 켜고 다른 창에서 일한다.
2. Claude가 `git push`를 실행하려 하고 허가가 필요하다.
3. 폰 화면이 주황색으로 점멸하고(안드로이드는 진동) 도구명과 명령이 카드로 뜬다.
4. 사용자가 **예 / 예, 다시 묻지 않기 / 아니오 / 터미널에서 응답할게요** 중 하나를 터치한다.
5. Claude가 즉시 진행(또는 거부)한다. "다시 묻지 않기"면 그 프로젝트에서 같은 종류의 명령은 더 묻지 않는다.

**S2. 상태 모니터링**
- 세션 목록에서 각 세션의 프로젝트명과 상태(작업 중 / 허가 대기 / 입력 대기 / 완료 / 종료됨)를 본다.
- Claude가 턴을 끝내면 "완료", 입력을 기다리면 "입력 대기"와 마지막 메시지 한 줄이 보인다.

**S3. 자리 비움 / 장애**
- 폰을 안 봐도 5분 후 터미널의 기본 프롬프트로 넘어간다.
- 데몬이 꺼져 있거나 죽으면 Claude Code는 평소와 완전히 똑같이 동작한다.

**S4. 다이얼 조작 (선택, tmux 필요)**
- 매크로패드 노브 또는 `/api/action`으로 확장 사고 토글, 모델 순환(`/model sonnet` ↔ `/model opus`), Escape를 보낸다.

## 5. 기능 요구사항

### 5.1 허가 응답 (P0)

| ID | 요구사항 | 상태 |
| --- | --- | --- |
| F-1 | `PermissionRequest` hook을 받아 데몬에 전달하고, 결정이 올 때까지 hook 프로세스를 대기시킨다 | 구현·검증 |
| F-2 | 대시보드에 도구명, 명령/경로/URL, 프로젝트명을 카드로 표시한다 | 구현·검증 |
| F-3 | **예** → `allow`, **아니오** → `deny`(사유 메시지 포함), **터미널에서 응답** → 무출력(passthrough) | 구현·검증 |
| F-4 | **항상 예** → 해당 프로젝트 `.claude/settings.local.json`의 `permissions.allow`에 규칙을 추가한 뒤 `allow` | 구현·검증 |
| F-5 | 규칙 생성: Bash는 `Bash(<명령> [<서브명령>] *)`, WebFetch는 `WebFetch(domain:<host>)`, 그 외 도구는 도구명. 규칙을 못 만들면 1회 승인으로 강등 | 구현·검증 |
| F-6 | 대기 타임아웃(기본 300초) 초과 시 passthrough | 구현 |
| F-7 | hook 연결이 먼저 끊기면(세션 강제 종료) 유령 요청을 즉시 정리 | 구현 |
| F-8 | 요청이 여러 개면 카드가 오래된 순으로 쌓이고, 각각 독립 응답 | 구현 |

### 5.2 상태 표시 (P0)

| ID | 요구사항 | 상태 |
| --- | --- | --- |
| F-9 | `SessionStart`/`SessionEnd` → 세션 카드 생성/종료 표시, 종료 후 5분 뒤 제거 | 구현·검증 |
| F-10 | `Stop` → 완료, `Notification(idle_prompt, agent_needs_input)` → 입력 대기 + 마지막 메시지 1줄 | 구현·검증 |
| F-11 | WebSocket으로 상태 변경 즉시 푸시, 초기 접속 시 스냅샷 전송, 끊기면 지수 백오프 재접속 | 구현·검증 |
| F-12 | 허가 대기 중 배경 점멸(0.5초 주기, 주황) + 최초 1회 진동(지원 기기) | 구현 |
| F-13 | 화면 꺼짐 방지(Wake Lock), iOS PWA 전체화면·safe-area 대응 | 구현 |

### 5.3 연결과 설치 (P0)

| ID | 요구사항 | 상태 |
| --- | --- | --- |
| F-14 | 데몬은 `127.0.0.1`에 항상 바인딩하고, 아이폰 USB 테더링(172.20.10.x)·안드로이드 테더링(192.168.42.x) 인터페이스는 10초 주기로 감지해 자동 바인딩/해제 | 구현 |
| F-15 | 안드로이드는 `adb reverse tcp:9200`을 30초 주기로 재시도. adb 미설치면 조용히 건너뜀 | 구현·검증 |
| F-16 | `scripts/install.sh` 한 번으로 의존성 설치, 대시보드 빌드, hook 등록을 끝낸다 | 구현·검증 |
| F-17 | hook 등록은 `~/.claude/settings.json`의 기존 hook을 보존하고, 재실행 시 중복 없이 갱신하며, 백업을 남긴다 | 구현·검증 |
| F-18 | yarn/corepack이 없는 환경(node 25+)에서도 설치가 완주한다 | 구현·검증 |
| F-19 | `cl` 셸 함수: 프로젝트별 tmux 세션에서 claude를 실행, 재실행 시 재접속, 인자 전달 | 구현 |

### 5.4 다이얼 액션 (P1, 선택)

| ID | 요구사항 | 상태 |
| --- | --- | --- |
| F-20 | `POST /api/action`으로 `thinking_down`/`thinking_up`/`model_cycle`/`escape`를 tmux pane에 주입 | 구현 |
| F-21 | 대상 세션은 지정된 세션 또는 "가장 최근 활동한, tmux pane을 아는 세션" | 구현 |
| F-22 | 매크로패드: Karabiner로 F19~F24를 `POST /api/key` 1~6으로 매핑. 1~3은 가장 오래 기다린 요청에 응답 | 구현 |

## 6. 비기능 요구사항

| ID | 요구사항 |
| --- | --- |
| N-1 | **Fail-open**: 데몬 미실행, 타임아웃, 파싱 실패 등 어떤 장애에서도 hook은 무출력 종료(exit 0)하여 Claude Code 기본 동작에 간섭하지 않는다 |
| N-2 | **보안**: 승인 API에 인증이 없으므로 루프백과 폰 USB 테더링 대역 외에는 절대 바인딩하지 않는다. `0.0.0.0` 설정 시 경고를 출력한다 |
| N-3 | **네트워크 독립**: 폰-맥 통신은 USB 터널만 사용한다. AP 격리망, 유심 없는 공기계에서도 동작 |
| N-4 | **팀 영향 없음**: "항상 예" 규칙은 gitignore 대상인 `settings.local.json`에만 기록한다 |
| N-5 | **최소 의존성**: 런타임 의존성은 `ws` 하나. tmux·adb·Karabiner는 선택 |
| N-6 | **상태 휘발**: 세션/요청 상태는 데몬 메모리에만 두고 영속화하지 않는다 (재시작 시 초기화가 안전) |

## 7. 인터페이스 요약

| 인터페이스 | 방향 | 내용 |
| --- | --- | --- |
| Claude Code hook | Claude → 데몬 | `PermissionRequest`, `Stop`, `Notification`, `SessionStart`, `SessionEnd`. `bin/hook-handler.js`가 stdin JSON + `$TMUX_PANE`을 `POST /hook/event`로 전달 |
| hook 출력 | 데몬 → Claude | `hookSpecificOutput.decision.behavior: allow \| deny` 또는 무출력 |
| `GET /ws`, `GET /api/state` | 데몬 → 폰 | `{type:"state", sessions:[...]}` 스냅샷 |
| `POST /api/respond` | 폰 → 데몬 | `{id, decision: once\|always\|deny\|passthrough}` |
| `POST /api/action`, `POST /api/key` | 폰/매크로패드 → 데몬 | 다이얼 액션, 키 번호 |
| `config.json` | 사용자 → 데몬 | 포트, 대기 시간, 모델 순환 목록, 토글 키, adb 주기 |

## 8. 검증 (2026-09-22)

- 데몬 단위: 예/항상 예/아니오/passthrough 4분기, allow 규칙 파일 기록, WebSocket 스냅샷, Stop/Notification/SessionEnd 상태 전이 모두 통과.
- 실기기 엔드투엔드: 실제 Claude Code 세션(`claude -p`, 프로젝트 스코프 hook)에서 Bash 허가 요청이 5초 내 데몬에 도착, API로 "예" 전송 후 명령 실행·세션 정상 종료 확인.
- 설치: node 26(corepack 없음) 환경에서 `install.sh`가 1단계에서 중단되던 문제를 npx corepack 폴백으로 해결하고 4단계 완주 확인.
- 미검증: 아이폰 USB 테더링 자동 바인딩, tmux 다이얼 주입(실기기·tmux 세션 필요).

## 9. 리스크와 열린 질문

- **hook 스키마 변동**: Claude Code 버전에 따라 `PermissionRequest` 입출력 형식이 바뀔 수 있다. 바뀌면 hook은 무출력으로 실패하므로 Claude Code는 안전하지만 기능은 조용히 죽는다 → 데몬 로그에 미인식 이벤트를 남겨 감지한다.
- **확장 사고 UI 변동**: 현재 `Meta+T` 토글 전제. 단계형으로 바뀌면 다이얼 좌/우 의미를 재정의해야 한다.
- **모델 정보 부재**: hook 페이로드에 모델이 없어 다이얼로 바꾼 경우에만 표시한다. 세션 초기 모델 표시는 미해결.
- **다중 사용자/원격**: 인증을 붙이지 않는 한 지원하지 않는다. 필요해지면 토큰 기반 인증 + 명시적 opt-in으로 별도 검토.

## 10. 향후 후보 (미확정)

- 대시보드에서 세션 선택 후 다이얼 액션 대상 지정 UI
- `updatedInput`을 이용한 "수정해서 허용"
- 허가 요청 이력(최근 N건) 표시
- 허가 요청이 오래 대기하면 폰 푸시 알림 대체 수단
